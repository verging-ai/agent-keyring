/**
 * Settings Feature Module
 *
 * Manages application settings including backup directory configuration
 * and other user preferences.
 *
 * @module features/settings
 */

export { useSettings } from "./hooks/useSettings";
